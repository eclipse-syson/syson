= 2026.9.0 (work in progress)

== Key highlights

== New features

* In diagrams:

** Add support for list item inheritance in _perform actions_ compartments.
** Add support for framed concern inheritance in _frames_ compartments of `RequirementUsage` and `RequirementDefinition` graphical nodes.
** Add support for `ItemUsage` inheritance in _items_ compartments of `PortUsage` and `PortDefinition` graphical nodes.
** Add support for assume constraint inheritance in _assume constraints_ compartments of `RequirementUsage` and `RequirementDefinition` graphical nodes.
** Add support for require constraints inheritance in _require constraints_ compartments of `RequirementUsage`, `RequirementDefinition` and subtypes graphical nodes.
** Add support for stakeholder inheritance in _stakeholders_ compartments of `RequirementUsage` and `RequirementDefinition` graphical nodes.
** Add support for `SatisfyRequirementUsage` inheritance in _satisfy requirements_ compartment of `PartUsage` and `PartDefinition` graphical nodes.
** Add support for objective requirement inheritance in _objective_ compartments of `CaseUsage` and `CaseDefinition` graphical nodes.
** Add support for actor inheritance in _actors_ compartment of `CaseDefinition` and `CaseUsage` graphical nodes.
The actor inheritance in _actors_ compartment is also visible in the following graphical nodes:

*** `RequirementDefinition`
*** `RequireUsage`
*** `ConcernDefinition`
*** `ConcernUsage`
*** `SatisfyRequirementUsage`
** Add support for subject inheritance in _subject_ compartment of `CaseDefinition` and `CaseUsage` graphical nodes.
The subject inheritance in _subject_ compartment is also visible in the following graphical nodes:

*** `RequirementDefinition`
*** `RequireUsage`
*** `ConcernDefinition`
*** `ConcernUsage`
*** `SatisfyRequirementUsage`
** Add support for the inheritance of the parameter border nodes of `ActionDefinition` and `ActionUsage` graphical nodes.

** Add _requirements_ compartments to `PartUsage` and `PartDefinition` graphical nodes.
+
image::release-notes-requirements-compartment-parts.png[Requirements compartments in PartUsage and PartDefinition graphical nodes, width=60%,height=60%]

** It is now possible to create new requirements using the _New Requirement_ tool from the `PartUsage` and `PartDefinition` graphical nodes.
+
image::release-notes-new-requirement-parts.png[New Requirement tool on PartUsage and PartDefinition graphical nodes, width=60%,height=60%]

** Add _concerns_ compartments to `PartUsage` and `PartDefinition` graphical nodes.
+
image::release-notes-concerns-compartment-parts.png[Concerns compartments in PartUsage and PartDefinition graphical nodes, width=60%,height=60%]

** It is now possible to create new concerns using the _New Concern_ tool from the `PartUsage` and `PartDefinition` graphical nodes.
+
image::release-notes-new-concern-parts.png[New Concern tool on PartUsage and PartDefinition graphical nodes, width=60%,height=60%]

** Requirement derivations are now displayed as edges in _General View_ diagrams.
A requirement derivation records that a requirement has been derived from another one, which is what allows a detailed requirement to be traced back to the requirement it comes from.
+
The edge goes from the derived requirement to the original one, and is displayed as a dashed line labelled `«derive»`, so that it can be told apart from a `«satisfy»` edge.
+
image::release-notes-requirement-derivation-edge.png[Requirement derivation displayed as an edge in a General View diagram]
+
Requirement derivations are created from SysML v2 text, using the _Upload model_ action of the _Explorer_ or its _New objects from text_ action.
The following text creates two requirements and a derivation between them:
+
[source,sysml]
----
private import RequirementDerivation::*;
requirement article5Compliance;
requirement paymentDelayPrevention;
#derivation connection {
    end #original ::> article5Compliance;
    end #derive ::> paymentDelayPrevention;
}
----
+
Once both requirements are displayed in a _General View_ diagram, by dropping them from the _Explorer_ for example, the `«derive»` edge between them appears automatically.
The direction of the edge is given by the `#original` and `#derive` metadata of its ends, so it does not depend on the order in which those ends are declared.


== Bug fixes

* In diagrams:

** Fix the missing graphical edge between `ConcernUsage` or `ConcernDefinition` graphical nodes and the Actors created from them.
** Fix dropping a nested `AttributeUsage` from the _Explorer_ onto a _General View_ diagram so it is visible on the first drop.
** Fix `Comment` graphical nodes so changing their background color no longer changes their border color.
** Fix the nested composition edge tool label on `ConcernUsage` graphical nodes in _General View_ diagrams.
It is now _Become nested Concern_ instead of _Become nested Requirement_.
** Fix the support of `PortUsage` as end inheritance in _ends_ compartment of `InterfaceDefinition` graphical node.
** Fix the ends of the connections whose ends are declared with the `end` keyword inside the body of the connection, which were not resolved.
Those connections were missing their source and their target, and were therefore never displayed as edges.
** Replace the name of the _New Perform action_ tool to _New Perform Action_ to be consistent with other tools.

* In all views:

** Fix expression creation and editing on `SuccessionAsUsage` elements.
** Fix the _Typed by_ property in the _Details_ view so a `Usage` can only be typed by its corresponding `Definition` kind.

* In textual import/export:

** Fix the export of the keywords carried by the ends of a connection, such as the `#original` and `#derive` of a requirement derivation, which were missing from the exported text when the connection had been created from a diagram.
Exporting such a connection and importing it back no longer loses which end is which.

== Improvements

* In diagrams:

* In textual import/export:

* In Requirements table view:

** The _Requirements table_ view now displays nested `Requirements`.
Nested `Requirements` are displayed in the table using a hierarchical way.
+
image::release-notes-hierarchical-requirements-table-view.png[Hierarchical requirements table view, width=90%,height=90%]

** On a `Requirement` row, its menu now includes a new action that allows users to create a nested `Requirement`
+
image::release-notes-hierarchical-requirements-table-view-create-nested-requirement.png[New sub requirement in row action menu, width=90%,height=90%]

== Technical details

* For technical details on this {product} release, including breaking changes, please refer to the https://github.com/eclipse-syson/syson/blob/main/CHANGELOG.adoc[changelog].
